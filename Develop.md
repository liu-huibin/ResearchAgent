# 智能科研助手（ResearchMate）分阶段开发方案

## 总体原则
- **渐进交付**：每个阶段产出可独立运行、验证的功能集。
- **降低风险**：先实现核心链路，逐步叠加复杂特性，避免“大爆炸式”集成。
- **验收驱动**：每阶段明确可测试的验收标准，确保成果可控。

---

## 阶段一：MVP（最小可行产品）—— 基础论文阅读与对话

**目标**：验证核心交互闭环——上传论文、在线阅读、流式对话、基本问答。

### 主要功能
1. **前端三栏布局**
   - 左侧：会话列表（新建/切换/重命名/删除）
   - 中间：PDF/Word 文档渲染与阅读
   - 右侧：对话界面（用户消息、Agent 回复）
2. **流式对话**
   - Agent 回复分为思考过程（灰色可折叠）与正文（黑色）
   - SSE 流式推送
3. **当前会话文档上传**
   - 支持上传 PDF/Word，绑定到当前会话，中间区域显示
4. **单 Agent 问答（ReaderAgent）**
   - 采用 ReAct 模式，可阅读当前会话文档文本并回答
   - 不含外部工具（仅内部解析文档文本）
5. **LLM 接入**
   - 管理员在服务端配置 API 参数
6. **会话持久化**
   - MySQL 存储会话与消息，刷新可恢复

### 后端组件
- FastAPI 基础框架
- LangGraph 简单图（单 Agent）
- SQLModel + MySQL 存储
- 文件上传与文本提取（PyPDF2 / python-docx）
- SSE 流式接口

### 前端组件
- React/Vue + TypeScript 脚手架
- pdf.js / mammoth.js 渲染
- 会话管理交互
- 流式对话 UI

### 不包含
- 知识库向量化与检索
- Multi-Agent 协作
- MCP 工具
- 混合检索
- 可观测性集成

### 验收标准
- 能创建/切换/重命名/删除会话
- 能上传 PDF/Word 并在中间区域阅读（缩放、翻页）
- 发送“总结这篇论文的方法”，Agent 以流式形式返回思考过程与正文
- 刷新页面后，会话列表和消息历史不丢失

---

## 阶段二：知识库与基础 RAG 检索

**目标**：构建长期知识库，让 Agent 能检索用户已上传文档，提供更广泛的知识支持。

### 主要功能
1. **知识库管理**
   - 左侧独立区域上传知识库文档，MD5 去重
   - 文件列表展示，支持删除
2. **向量化与存储**
   - 文档切片（500 tokens，50 重叠）
   - 使用 Embedding 模型向量化，存入 Chroma
3. **基础检索工具**
   - 语义向量检索（Top-K 召回）
   - Agent 可调用该工具获取知识库片段
4. **前端增强**
   - 上传进度显示
   - 引用标注：答案中引用来源时，中央阅读区高亮对应段落

### 后端增量
- ChromaDB 集成
- 文档切片与 Embedding 服务
- 检索工具（`retrieve_knowledge`）
- MD5 去重逻辑

### 验收标准
- 上传 3 篇 PDF 到知识库，重复文件被拒绝
- 提问“知识库中关于 XX 的研究有哪些？”，Agent 能返回相关片段并指明来源
- 点击引用可跳转到对应文档段落

---

## 阶段三：混合检索与 Rerank 精排

**目标**：提升检索质量，结合关键词与语义，兼顾查全与查准。

### 主要功能
1. **BM25 索引构建**
   - 对知识库切片建立 BM25 索引
2. **多路混合检索**
   - 语义检索召回 + BM25 召回，合并去重
3. **Rerank 精排**
   - 集成 Rerank 模型（如 bge-reranker-v2-m3）
   - 对候选片段重新排序，输出最终 Top-K
4. **工具升级**
   - 替换原检索工具为 `hybrid_retrieve`，Agent 统一调用

### 后端增量
- BM25 索引（rank_bm25 或 Whoosh）
- Rerank 模型服务（本地或 API）
- 混合检索流水线实现

### 验收标准
- 输入含专业术语的查询，返回结果明显优于纯向量检索
- 检索总延迟 < 1.5 秒
- 混合检索结果排序合理，相关性高的排在前面

---

## 阶段四：Multi-Agent 协作系统

**目标**：实现多 Agent 分工协作，支持“阅读→思路生成→审查”的完整科研工作流。

### 主要功能
1. **Multi-Agent 图（LangGraph）**
   - Supervisor：任务分解与调度
   - ReaderAgent：论文阅读与分析
   - IdeationAgent：科研思路生成
   - ReviewerAgent：内容审查与事实核查
2. **协作流程**
   - Supervisor 根据意图路由到子 Agent
   - 支持串联调用（如 Reader → Ideation → Reviewer）
3. **ReAct 循环上限**
   - 设置最大迭代次数（默认 10），防止无限循环
4. **前端展示**
   - 显示当前活跃 Agent 名称
   - 工具调用与审查过程可视化

### 后端增量
- LangGraph 状态图实现
- 各 Agent 的 Prompt 与工具绑定
- 审查逻辑与修正回路

### 验收标准
- 输入“阅读这篇论文并提出 3 个改进思路”，系统依次执行：
  - ReaderAgent 输出论文总结
  - IdeationAgent 生成思路
  - ReviewerAgent 审查并给出意见
- 过程中能看到不同 Agent 的思考内容
- 整体响应可超 30 秒但不丢失状态，超时或达到循环上限能优雅终止

---

## 阶段五：MCP 工具集成与可观测性

**目标**：标准化工具接入，提升系统可维护性与透明性。

### 主要功能
1. **MCP 文件读取工具**
   - 实现 MCP Server，安全访问 `/data/documents/` 目录
   - Agent 通过 MCP Client 调用 `read_file` 工具
2. **可观测性集成**
   - 接入 LangSmith 或 LangFuse，记录全链路追踪
   - 提示词版本管理与 A/B 测试能力
3. **监控与成本统计**
   - 记录各 Agent Token 消耗
   - 可选告警（循环次数、失败率等）

### 后端增量
- MCP Server / Client 实现（mcp-python-sdk）
- LangSmith 回调集成
- 日志与监控埋点

### 验收标准
- Agent 可通过 MCP 成功读取 `/data/documents/` 下的指定文件
- LangSmith 中能查看完整的 Agent 推理链与工具调用过程
- 可在面板查看单次对话的 Token 消耗（预留）

---

## 阶段六：优化与扩展（可选）
- **多用户支持**：基于现有预留字段扩展登录与权限
- **性能优化**：并行检索、缓存、流式优化
- **更多工具**：ArXiv 搜索等工具接入
- **前端体验**：暗色模式、响应式适配、移动端支持

---

## 阶段依赖关系

```text
阶段一（MVP）
  └→ 阶段二（知识库 + 基础 RAG）
        └→ 阶段三（混合检索 + Rerank）
              └→ 阶段四（Multi-Agent 协作）
                    └→ 阶段五（MCP + 可观测性）
                          └→ 阶段六（优化扩展）
```
            
- 每个阶段在上一个稳定版本基础上增量开发，不会破坏已有功能，符合独立验证原则。